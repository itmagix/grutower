apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: grutower
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: grutower
          servicePort: 80
